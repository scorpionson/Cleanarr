certifi==2024.12.14
chardet==5.2.0
Click==8.1.7
Flask==3.0.2
# 3.0.9 is affected by CVE-2024-6221 (Access-Control-Allow-Private-Network)
Flask-Cors==4.0.0
idna==3.7
itsdangerous==2.2.0
Jinja2==3.1.4
MarkupSafe==2.1.5
# Deliberately NOT downgraded - upstream PR #154 moves this to 4.14.0, which is
# older than what shipped here.
PlexAPI==4.15.5
requests==2.32.3
six==1.16.0
tinydb==4.8.0
tqdm==4.66.4
urllib3==2.2.1
websocket-client==1.8.0
Werkzeug==3.0.3
python-dotenv==1.0.1
pytest-benchmark==4.0.0
# Production WSGI server. The base image used to provide uwsgi+nginx; running
# `flask run` instead (as PR #154 does) means shipping Werkzeug's development
# server, which is single-threaded and explicitly not for production use.
gunicorn==22.0.0
